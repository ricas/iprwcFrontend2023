export const environment = {
  production: false,
  server: 'http://localhost:8080/api',
  endpoints: {
    products: "/products",
    user: "/users",
    userProfile: "/users/profile",
    order: "/orders",
    login: "/login",
    register: "/register"
  }
};
